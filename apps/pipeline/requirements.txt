# NOTE: soa_shared/ is copied from packages/shared/soa_shared/ for
# Railway deployment. The local path install only works in local dev.
# When updating shared models, run:
#   cp -r packages/shared/soa_shared apps/pipeline/soa_shared
# Same pattern is used in apps/api/

# All original /soa dependencies:
SQLAlchemy==2.0.46
alembic>=1.13,<2.0.0
psycopg2-binary==2.9.11
python-dotenv==1.2.1

# LLM clients — openai pinned to 1.x to avoid 2.x pulling numpy/sounddevice
openai>=1.57.0,<2.0.0
google-genai>=1.0.0,<2.0.0
openpyxl>=3.1.0,<4.0.0

# HTTP — pinned for compatibility with openai 1.x and httpcore 1.x
httpx>=0.27.0,<0.29.0
httpcore>=1.0.0,<2.0.0
